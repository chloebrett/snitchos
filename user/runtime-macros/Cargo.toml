[package]
name = "snitchos-user-macros"
version = "0.0.1"
edition = "2024"
publish = false

# Proc-macros for the SnitchOS userspace runtime. Compiles for the host (all
# proc-macros do); no dependencies — the one macro is hand-rolled over
# `proc_macro::TokenStream` rather than pulling in syn/quote for a single
# attribute.

[lib]
proc-macro = true

[dependencies]
