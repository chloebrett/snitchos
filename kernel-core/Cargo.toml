[package]
name = "kernel-core"
version = "0.0.1"
edition = "2024"
publish = false

[dependencies]
protocol = { path = "../protocol" }
postcard = { version = "1", default-features = false }

[lints]
workspace = true
