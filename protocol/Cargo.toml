[package]
name = "protocol"
version = "0.0.1"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"] }
postcard = { version = "1", default-features = false }
