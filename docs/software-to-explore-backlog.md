# 📚 Software to explore — backlog

# Discussed so far
Notes captured in **Software explorations — notes**:

- How QEMU works (TCG vs KVM/HVF, device emulation, virtio)
- Devices and drivers (the keyboard driver walkthrough)
- Terminals (emulators, escape codes, PTYs, PuTTY, TUIs/ratatui)
- Window managers and compositors
- Frame timing (tearing, vsync, double/triple buffering, damage)
- Databases and query planning
- Container runtimes (Docker, and the SnitchOS version)

Also discussed earlier in the project: browsers, text rendering, the `init`/systemd topic.

# Still on the menu (suggested priority)
The **shell**, the **debugger**, the **dynamic linker** remain high-relevance and undiscussed.
