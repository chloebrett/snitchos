# v0.12 — Notification primitive (the general async kernel→user signal)

**Status (2026-06-27): SHIPPED + verified.** All six increments below landed,
closing v0.12 edge #2 ("general notification primitive") from
[v0.12-exit-wait.md](v0.12-exit-wait.md). Every host-testable layer is
mutation-clean; the `notify-signal-wakes-waiter` itest runs the async wake end to
end (0/10 flakes on `--repeat 10`). Design: [docs/notification-design.md](../../docs/notification-design.md).

**Option A** shipped: the generic `Notification` object proven by a *fresh*
minimal consumer (the `notify-smoke` workload, not child-exit); child-exit stays
on its existing `ReapTable` path and folds onto notifications (Option B) in a
later milestone, after the device-IRQ consumer validates the generic shape.

**Syscall numbers as built:** `NotifyCreate = 21`, `Signal = 22`, `WaitNotify = 23`
(the plan first guessed 19–21; `ConsoleWrite = 19` / `ClockNow = 20` had since
taken those, so the three appended past `ClockNow`). Syscall numbers aren't frozen
— appending is free — so this was a no-cost correction.

## The object in one paragraph
A `Notification` is one machine word of pending-signal bits plus at most one
parked waiter. `Signal` OR-s a mask into `pending` and wakes any waiter (never
blocks). `WaitNotify` returns-and-clears `pending` if nonzero, else parks the
caller and blocks. Coalescing falls out (N signals before a wait collapse to one
wake of OR-ed bits) → no queue, no per-signal kernel memory. Cap-gated by
disjoint `SIGNAL`/`WAIT` rights. Every signal/wake is a wire frame.

## TDD discipline (per project rules)
Each increment is **RED first, in its own edit** (failing test), then the
minimum GREEN, then MUTATE (`cargo xtask mutants` on the touched core) and kill
survivors, then assess refactor. Never batch the test and impl into one edit,
even when the pattern feels obvious. Host-testable logic lands in `kernel-core`
and is covered by `cargo test -p kernel-core`; kernel-side asm/CSR/MMIO wiring is
covered transitively by the QEMU itest.

---

## Increment 1 — `kernel_core::notify` pure core (host-tested) ✅ SHIPPED
*7 tests, mutation-clean (6 caught / 5 unviable / 0 survived). `WaitStep::Busy`
encodes the second-waiter rejection (the edge-#3 lesson) up front.*

Mirror `kernel_core::reap` exactly: pure data + bookkeeping, no `unsafe`, no MMIO.

New file `kernel-core/src/notify.rs`, exported from `lib.rs`.

```rust
pub enum SignalStep { Woke(TaskId), NoWaiter }
pub enum WaitStep   { Ready(u64), Block }

pub struct Notification { pending: u64, waiter: Option<TaskId> }

impl Notification {
    pub const fn new() -> Self;
    pub fn signal(&mut self, mask: u64) -> SignalStep; // pending |= mask; Woke(w) if a waiter is parked, else NoWaiter
    pub fn wait(&mut self, caller: TaskId) -> WaitStep; // pending!=0 → Ready(take pending, clear); else park caller, Block
}
```

RED→GREEN test list (one test per behaviour, each written before its code path):
1. `wait_with_no_pending_blocks` → `Block`, caller recorded as waiter.
2. `signal_then_wait_returns_the_bits` → `signal(0b1)` then `wait` → `Ready(0b1)`.
3. `wait_clears_pending` → after a `Ready` wait, a second `wait` → `Block` (read-and-clear, not re-report).
4. `signals_coalesce` → `signal(0b001)`, `signal(0b100)` before any wait → one `wait` → `Ready(0b101)`.
5. `signal_with_a_parked_waiter_wakes_it` → `wait` (Block), then `signal(0b1)` → `SignalStep::Woke(that_caller)`; the bits are pending for the woken task's re-`wait`.
6. `signal_with_no_waiter_is_noop_wake` → `signal` with nobody parked → `NoWaiter`, bits still pending.
7. `second_waiter_is_rejected` → one waiter parked, a *different* task `wait`s → distinct outcome (NOT silent overwrite). **Decision to encode here:** return a `WaitStep::Busy` (or `Refused`) variant rather than overwriting `waiter` — this is the lesson from `ReapTable.waiters`' single-slot overwrite (v0.12 edge #3). The kernel maps `Busy` → `SyscallRefused`.

MUTATE: run mutants on `notify.rs`; the OR-accumulate, the take-and-clear, and the waiter-occupied check are the load-bearing lines — confirm each has a killing test.

## Increment 2 — `NotificationId` + the cap object + rights bits ✅ SHIPPED
*Split into two committable slices: 2a (rights bits, independently host-green,
no cascade) then 2b (the `Object::Notification` variant, which the compiler
coupled to the `protocol::CapObject::Notification` wire mirror via the exhaustive
`Object → CapObject` map in `trap/user.rs`). `NotificationId` lives in
`kernel-core/src/notify.rs` (the notify module owns its id type, like
`EndpointId` lives in `ipc`).*

- `kernel-core/src/user/cap.rs`: add `Object::Notification { id: NotificationId }` and `NotificationId` (new id newtype, like `EndpointId`).
- `abi::rights`: add `SIGNAL` and `WAIT` bits (next free past `MINT = 0b1000`, i.e. `0b1_0000` / `0b10_0000`); add `Rights::SIGNAL` / `Rights::WAIT` consts referencing them.
- `Multiplicity::Persistent` (default — no change needed; just assert it in a test).

RED first: a `CapTable` test that resolves a `Notification` cap and that `SIGNAL`/`WAIT` are disjoint (`SIGNAL.contains(WAIT)` is false, etc.), then add the variant/bits to pass.

Also: `protocol::stream::OwnedFrame::from_borrowed` and any `CapEventKind::Object` mirror must gain the `Notification` arm — tests fail to compile until the wire mirror is updated (the documented "add a variant" workflow).

## Increment 3 — the live table + kernel block/wake wiring ✅ SHIPPED (regrouped)
*The host-testable `NotifyTable` (6 tests, mutation-clean: 6 caught / 5 unviable
/ 0 survived) shipped as its own slice. The **kernel** `NOTIFY` static +
`notify_create`/`notify_signal`/`notify_wait` glue had no caller until the
syscalls, so it was **folded into increment 4** (dead code otherwise; no
independent test). The glue lives in `kernel/src/sched/mod.rs` next to `REAP`, not
a separate `kernel::notify` module.*

Kernel side owns the live notifications behind a `Mutex`, exactly as `REAP` owns `ReapTable`.

- A `NOTIFY` static: `Mutex<NotifyTable>` mapping `NotificationId → Notification` (+ an id allocator). `NotifyTable` itself is host-testable in `kernel-core` (the map + allocate; reuse the `BTreeMap` pattern from `reap`).
- `kernel::sched` (or a small `kernel::notify` module): `signal(id, mask)` → on `Woke(w)` call `sched::wake(w)`; `wait(id, caller)` → on `Block` call `block_current()`, on `Ready(bits)` return the bits. Reuses `block_current`/`wake` **verbatim** — no new park/unpark machinery.

Covered transitively by the itest in increment 6 (no host test for the asm-adjacent wiring; the pure `NotifyTable` map logic gets its own kernel-core tests).

## Increment 4 — syscalls + kernel glue: `NotifyCreate` / `Signal` / `WaitNotify` ✅ SHIPPED
*Merged with the increment-3 kernel glue (above). The cap-invocation decisions
`invoke_signal`/`invoke_wait` are pure + host-tested in `kernel-core::cap`
(mirroring `invoke_send`/`invoke_recv`; 6 tests, mutation-clean: 2 caught / 2
unviable / 0 survived). A new `RefusalReason::NotificationBusy` (appended) names a
refused second waiter on the wire. The kernel dispatch/asm path is proven by the
increment-6 itest.*

Append to `abi::Syscall` past `ClockNow = 20` (numbers **21/22/23** — appending is free; syscall numbers are not frozen, only the postcard frame format is). Add `from_usize` arms (host-tested in `abi`).

Dispatch in `kernel/src/syscall/` (new `notify.rs`, wired into the trap dispatch):
- `NotifyCreate` (21): mint a fresh `SIGNAL|WAIT` `Notification` cap into the caller's table; return the handle in `a0`. Ambient (making your own notification needs no prior cap), like `MapAnon`.
- `Signal` (22): resolve `a0` cap, require `SIGNAL`, OR `a1` mask, wake any waiter, `a0 = 0`. Refuse (snitch `SyscallRefused`) on bad handle / missing right.
- `WaitNotify` (23): resolve `a0` cap, require `WAIT`; `Ready(bits)` → `a0 = bits`; `Block` → `block_current()` (return value set on wake); `Busy` (second waiter) → refuse + snitch `NotificationBusy`.

Every refusal path emits `SyscallRefused` + bumps the refusal counter — the project invariant, never a silent failure.

## Increment 5 — wire frames + runtime bindings ✅ SHIPPED
*Frames carry `t` + `hart_id` like every other variant. The compiler-forced
mirror cascade was wider than `OwnedFrame`: `collector::state` (anchor + `None`,
like `Message`) and the xtask `harness` capture-formatter + `variant_name` also
matched `Frame`/`OwnedFrame` exhaustively. Runtime bindings shipped as a
`Notification { handle }` wrapper (`signal`/`wait`/`raw_handle`) + ambient
`notify_create() -> Notification`, mirroring `Endpoint` rather than free
functions.*

- `protocol`: add `Frame::NotifySignal { notification, mask, from_task, t, hart_id }` and `Frame::NotifyWait { notification, bits, to_task, t, hart_id }` (**appended** — never reorder existing variants; postcard is positional). Roundtrip tests in `protocol` (the established `Frame` test pattern). Update `OwnedFrame::from_borrowed` arms.
  - Frame-budget note from the design doc: the design suggested emitting `NotifySignal` only on the empty→nonempty transition + each wake. **As built, we emit per `Signal` syscall** — simpler, and correct for v0.12's single low-rate consumer. The empty→nonempty optimization matters only for a future high-rate IRQ source; the rationale is flagged in the `NotifySignal` doc comment for that day.
- `user/runtime/src/lib.rs`: a `Notification` wrapper + `notify_create() -> Notification` — siblings of the existing `Endpoint` / `wait(child) -> i32` bindings.

## Increment 6 — itest scenario (the proof on the wire) ✅ SHIPPED
*Passes, 0/10 flakes on `--repeat 10` (292 ms mean). The two tasks are two
**userspace processes** (notification syscalls are U-mode): a `notify-waiter`
parent (boot, `workload=notify-smoke`) and a `notify-signaller` child (SPAWNABLE
id 2). The parent `notify_create`s, `spawn`s the child delegating the **whole**
cap, then `WaitNotify`s; the child `Signal`s `0b101`. Delegating the whole cap
(child holds `SIGNAL|WAIT` but only signals) is enough to prove the cross-task
wake — the two-ended `SIGNAL`/`WAIT` split is covered at the host level by
`invoke_signal`/`invoke_wait`.*

New `workload=notify-smoke` (kernel-core `WorkloadKind` variant + parse arm, host-tested; dispatch in `kmain`/spawn layout per the runtime-workload pattern).

Scenario `notify-signal-wakes-waiter` in `xtask/src/itest/scenarios.rs` (registered in `itest.rs::SCENARIOS`) asserts, in wire order: `ThreadRegister "notify-signaller"` → `NotifySignal{mask=0b101}` → `NotifyWait{bits=0b101}` → `Metric snitchos.notify.bits == 5`. The last ties it down hardest: the parent's `WaitNotify` syscall *returned* the bits to userspace, not just onto the wire. The post angle made testable: the async edge a synchronous trace can't show.

Gate: `cargo xtask itest --repeat 10` clean (single-run-green hid the v0.6 flakes) — **met, 0/10**.

---

## Out of scope (documented growth points, not silent gaps)
- **Folding child-exit onto the notification (Option B)** — after the device-IRQ consumer validates the generic shape.
- **Multi-waiter fan-out** (one signal wakes N) — needs a waiter list, not a slot; increment 1's `Busy` rejection is the placeholder invariant.
- **Badged notifications** (distinguish *who* signalled) — the signal mask covers single-process demux without it.
- **Non-blocking `poll`** flag on `WaitNotify` — additive when an event loop over several sources needs it.
- **IRQ→notification binding** — the kernel trap handler signalling a notification a driver waits on. The *real* point of the primitive; lands with device drivers (post-v1.0 HAL/arcade arc), reusing this object unchanged.

## Done means — all met ✅
- ✅ `kernel_core::notify` + `NotifyTable` host-tested and mutation-clean.
- ✅ `Object::Notification` + `SIGNAL`/`WAIT` rights; wire mirror updated.
- ✅ Three syscalls + runtime bindings; every refusal snitches (incl. `NotificationBusy`).
- ✅ Two wire frames, roundtrip-tested.
- ✅ `notify-signal-wakes-waiter` itest green; `--repeat 10` clean (0/10).
- ⏳ **Remaining:** mark edge #2 SHIPPED in `v0.12-exit-wait.md`; draft the devlog post ("the kernel learns to tap you on the shoulder" / the async-edge-in-Tempo angle). The smaller v0.12 cleanup edges (#3 one-waiter — invariant *enforced* via `WaitStep::Busy`, wants its own itest; #4 parent/child enforcement; #5 scenario rename) are separate slices.
