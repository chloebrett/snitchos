# Candidate 007

| | |
|---|---|
| Model | qwen3.5-4b |
| Prompt | [v5](../corpus-prompts/v5.md) |
| Exemplars | text.st + stats.st |
| Cost | ~3.5 min |
| Thinking | on (no working off-switch found) |

Program: [`007.st`](007.st)

**Summary.** Type error: `overlap` takes `Booking`s, called with `Time`s — a helper was deleted while consolidating and the call site was not updated.

Full analysis: [corpus-mvp-spike.md](../corpus-mvp-spike.md) Findings 007.

## Reasoning trace

```
The user wants me to write a sauna booking module in Stitch based on the reference provided.

Key constraints from the prompt:

Write a sauna booking module.
Rooms are booked for exclusive use over a time window.
Core problem is detecting when two bookings overlap.
Shape: a module (no main), 1 to 4 types, 2 to 6 functions (excluding tests).
Use constructs: prod, Maybe, |>.
Reply with exactly one fenced ```stitch block and nothing else.
I need to implement:

A type for a Booking (start time, end time, room ID).
A function to check if two bookings overlap.
Maybe a helper to list available rooms or something similar.
Use prod for data types.
Use Maybe for optional values (e.g., result of a lookup).
Use pipes |> for composition.
Follow Stitch syntax rules (no loops, no if/else, use cond or match, ext for exports, etc.).
Let's plan the module:

Types:

prod Time(start: Int, end: Int)
prod Booking(id: Int, time: Time)
sum BookingStatus = Confirmed | Cancelled (optional, maybe keep it simple)
Maybe just stick to Booking and check overlap.
Functions:

overlap(b1: Booking, b2: Booking) -> Bool
book(roomId: Int, time: Time) -> Maybe<Booking> (if room exists)
isAvailable(roomId: Int, time: Time) -> Bool (check against existing bookings)
Tests:

Test overlap logic.
Test availability logic.
Stitch specifics:

prod for data types.
ext for public items.
match or cond for conditionals.
|> for pipes.
Maybe<T> = Some(T) | None.
Booleans are words and, or, not.
Let's draft the code.

// Sauna booking module
// Core logic: detecting overlapping time windows

prod Time(start: Int, end: Int)
prod Booking(id: Int, time: Time)
prod BookingStatus = Confirmed | Cancelled

ext overlap(b1: Booking, b2: Booking) -> Bool = {
    let t1 = b1.time
    let t2 = b2.time
    // Two bookings overlap if start1 < end2 AND start2 < end1
    t1.start < t2.end and t2.start < t1.end
}

// Check if a room is free during the requested time window against a list of bookings.
// Returns None if conflict, Some(Booking) if available.
ext canBook(roomId: Int, time: Time, existingBookings: List<Booking>) -> Maybe<Bool> = {
    fold(existingBookings, true, (available, b) ->
        match available {
            true => not(overlap(b.time, time)) | false
            false => false
        })
}

// Simpler helper: check overlap between two times.
ext timesOverlap(t1: Time, t2: Time) -> Bool = t1.start < t2.end and t2.start < t1.end

// Test suite
test "overlap detects same time" {
    expect overlap(Booking(id: 1, time: Time(start: 10, end: 20)), Booking(id: 2, time: Time(start: 10, end: 20))) == true
}

test "overlap detects partial overlap" {
    expect overlap(Booking(id: 1, time: Time(start: 10, end: 20)), Booking(id: 2, time: Time(start: 15, end: 30))) == true
}

test "overlap detects no overlap" {
    expect overlap(Booking(id: 1, time: Time(start: 10, end: 20)), Booking(id: 2, time: Time(start: 20, end: 30))) == false
}

// ... maybe more tests
Wait, I need to check the Stitch syntax carefully.

prod definition: prod Type(name: Type)
ext before prod for exported types? The reference says "Exporting a type: ext goes BEFORE prod/sum, and every field that should be visible needs its own ext too."
So I need ext prod Time(ext start: Int, ext end: Int).
prod for Booking: ext prod Booking(id: Int, time: Time).
cond vs match: Reference says cond => a | b or match { ... }. match is often better for multiple patterns.
Booleans are and, or, not. No &&.
Maybe<T> = Some(T) | None.
fold is used.
List is imported.
Let's refine the types and exports.

use List
use text // not needed for this task, but reference mentions it

// Types
ext prod Time(ext start: Int, ext end: Int)
ext prod Booking(id: Int, time: Time)

// Logic
ext overlap(b1: Booking, b2: Booking) -> Bool = {
    let t1 = b1.time
    let t2 = b2.time
    t1.start < t2.end and t2.start < t1.end
}

ext checkConflict(bookings: List<Booking>, newTime: Time) -> Bool = {
    fold(bookings, false, (conflict, b) -> conflict or overlap(b.time, newTime))
}

// Test
test "overlap detects same time" { ... }
Wait, the prompt says "Write a sauna booking module". I should probably include a way to get a room ID or something simple. But the core is overlap detection.
Let's stick to the overlap logic and availability check.

Refining syntax:

prod needs ext prefix on fields if exported? The reference says "every field that should be visible needs its own ext too". Since Time fields are used in functions, they might not need to be ext, but the type itself is ext.
Actually, looking at ext prod Summary(ext count: Int, ext total: Int), the fields count and total are ext.
So I will make Time fields ext.
Let's check fold. fold(xs, init, f).
f takes (acc, x) -> ....
If I use cond, it's cond => then | else.

One detail: "Reply with exactly one fenced ```stitch block and nothing else."

I should ensure the module is self-contained (imports used).
I need to make sure I don't use if or return.
I need to use match for complex logic.

Let's write the code.

use List

// Types
ext prod Time(ext start: Int, ext end: Int)
ext prod Booking(id: Int, time: Time)

// Core overlap logic
// Two bookings overlap if one starts before the other ends and vice versa
ext overlap(b1: Booking, b2: Booking) -> Bool = {
    let t1 = b1.time
    let t2 = b2.time
    t1.start < t2.end and t2.start < t1.end
}

// Check if a new booking conflicts with any existing bookings in the list
ext isAvailable(bookings: List<Booking>, time: Time) -> Bool = {
    fold(bookings, false, (available, b) ->
        match available {
            true => overlap(b.time, time) | false
            false => false
        })
}

// Helper to create a booking
ext createBooking(id: Int, time: Time) -> Booking = Booking(id: id, time: time)

// Test suite
test "overlap detects exact same time" {
    let b1 = createBooking(1, Time(start: 10, end: 20))
    let b2 = createBooking(2, Time(start: 10, end: 20))
    expect overlap(b1, b2) == true
}

test "overlap detects partial overlap" {
    let b1 = createBooking(1, Time(start: 10, end: 20))
    let b2 = createBooking(2, Time(start: 15, end: 30))
    expect overlap(b1, b2) == true
}

test "overlap detects no overlap" {
    let b1 = createBooking(1, Time(start: 10, end: 20))
    let b2 = createBooking(2, Time(start: 20, end: 30))
    expect overlap(b1, b2) == false
}

test "availability finds conflict" {
    let existing = [createBooking(1, Time(start: 10, end: 20))]
    let available = isAvailable(existing, Time(start: 5, end: 15))
    expect available == false
}

test "availability finds no conflict" {
    let existing = [createBooking(1, Time(start: 10, end: 20))]
    let available = isAvailable(existing, Time(start: 25, end: 30))
    expect available == true
}
Wait, Time fields need to be exported if I want to access them easily. In the reference example: ext prod Summary(ext count: Int, ext total: Int). So I did that.
The prod keyword itself is not exported, but the type name is used in the signature.
Actually, ext prod Type means the type is exported.
Wait, the reference says: "Exporting a type: ext goes BEFORE prod/sum, and every field that should be visible needs its own ext too."
So ext prod Time(ext start: Int, ext end: Int) is correct.

One thing: fold on an empty list returns init.
My isAvailable logic: fold(bookings, false, ...). If bookings is empty, it returns false (meaning conflict). Wait, if no bookings exist, should it be available?
Yes, false means "conflict found". So if list is empty, false is correct (no conflict).
Wait, I want to return true if available.
So init should be true.
If bookings is empty, returns true.
If any conflict, returns true. Wait.
Logic:
fold(xs, true, (available, x) -> not(conflict(x)) or available)? No.
fold accumulates.
fold([b1], true, (avail, b) -> avail and overlap(b.time, newTime))
If bookings is empty -> returns true.
If b1 conflicts -> returns false.
So init should be true.

Let's fix isAvailable:

ext isAvailable(bookings: List<Booking>, time: Time) -> Bool = {
    fold(bookings, true, (available, b) ->
        match available {
            true => not(overlap(b.time, time)) | false
            false => false
        })
}
If bookings is empty, returns true.
If b1 conflicts, overlap is true. not(true) is false. Result: false and ... -> false. Correct.

Wait, I need to check the reference for fold usage in summarise:

total(xs) = fold(xs, 0, (acc, x) -> acc + x)
This sums up numbers.
My isAvailable uses Bool. and/not work on Booleans.

One detail: prod vs sum. Time is a record with fields. prod is correct for Time. Booking is also prod.
The reference uses prod Point(x: Int, y: Int).
So ext prod Time(ext start: Int, ext end: Int) is correct.

Wait, use List is needed for List<Booking>.

One more thing: The prompt asks for "1 to 4 types and 2 to 6 functions".
I have:
Types: Time, Booking (2 types).
Functions: overlap, isAvailable, createBooking, test ... (Tests are not counted).
So 3 functions + tests. This fits "2 to 6 functions".

Wait, I should check if createBooking is needed. It's just a helper.
The prompt says "Write a sauna booking module".
I'll include it.

Check imports: use List.

Check syntax:

prod definition.
ext prefix on types and functions.
and, or, not.
match or cond.
fold.
|> pipe? I used fold directly but could use |> to make it clearer, but fold is a function call in Stitch. The prompt explicitly says "Use these constructs: prod, Maybe, |>". I haven't used Maybe yet. The reference example uses Maybe for optional values. I should probably include a function that returns Maybe<Booking> or similar to satisfy the requirement to use Maybe. Or I can change isAvailable to return Maybe<Bool>? No, it's a boolean check. Better: Add a function that creates a booking and checks availability, returning Maybe<Error> or something. Actually, the prompt says "Use these constructs: prod, Maybe, |>". It lists them as things I should use. I will add a book function that returns Maybe<Booking>.
ext book(roomId: Int, time: Time) -> Maybe<Booking> = {
    let b = createBooking(roomId, time)
    match fold([b], true, (available, x) -> ...)
}
Wait, I need to pass existing bookings.
Let's make book take a list of existing bookings implicitly? No, that's messy.
Let's add a function findFreeRoom(roomId: Int, time: Time) which returns Maybe<Booking> if available.

Actually, simpler:

ext tryBook(existingBookings: List<Booking>, roomId: Int, time: Time) -> Maybe<Bool> = {
    match fold(existingBookings, true, (avail, b) -> avail and not(overlap(b.time, time))) {
        true => Some(true)
        false => None
    }
}
Wait, Maybe is defined as sum Maybe<T> = Some(T) | None.
So I need to use it.

Let's adjust the code to ensure Maybe and |> are used meaningfully.
I'll add a function that returns Maybe<Bool> indicating success.
And use pipes in the test or helper.

Actually, I can just use |> in fold or list operations if I want.
But fold is native. map, filter etc are available.
Let's use |> in isAvailable.

ext isAvailable(bookings: List<Booking>, time: Time) -> Bool = {
    bookings |> fold(true, (avail, b) -> avail and not(overlap(b.time, time)))
}
Wait, fold expects (acc, x) -> ....
If I use pipes: bookings |> fold(true, ...).
The pipe supplies the first argument to fold. So fold(bookings, true, f) becomes f(bookings, true).
Actually xs |> fold(init, f) is fold(xs, init, f).
So I can write:

ext isAvailable(bookings: List<Booking>, time: Time) -> Bool = {
    bookings |> fold(true, (avail, b) -> avail and not(overlap(b.time, time)))
}
I will add a book function that returns Maybe<Bool> to use Maybe.
Or I can make tryBook return Maybe<Booking>? No, isAvailable returns Bool.
Let's add a check function that returns Maybe<Bool>.

Wait, the prompt says "Use these constructs: prod, Maybe, |>".
I should ensure all three are used.

prod: Used for Time and Booking.
Maybe: I will use it in tryBook.
|>: Used in isAvailable.
Wait, Maybe is a type alias sum Maybe<T> = Some(T) | None.
I can just define ext prod Booking and use Maybe<Bool> in signature.

Let's write the final code.

use List

// Sauna booking module
// Overlap detection for exclusive room usage

// Types: Time is a record of start/end, Booking holds ID and Time
ext prod Time(ext start: Int, ext end: Int)
ext prod Booking(id: Int, time: Time)

// Core overlap logic: two bookings conflict if they share any time
ext overlap(b1: Booking, b2: Booking) -> Bool = {
    let t1 = b1.time
    let t2 = b2.time
    t1.start < t2.end and t2.start < t1.end
}

// Check if a new booking conflicts with any existing bookings
// Uses pipes and fold for composition
ext isAvailable(bookings: List<Booking>, time: Time) -> Bool = {
    bookings |> fold(true, (avail, b) -> avail and not(overlap(b.time, time)))
}

// Attempt to book a room, returns Maybe indicating success
// Uses Maybe<T> = Some(T) | None
ext tryBook(existingBookings: List<Booking>, roomId: Int, time: Time) -> Maybe<Bool> = {
    if isAvailable(existingBookings, time) then Some(true) else None
}
Wait, I used if in tryBook. I cannot use if.
I must use match or cond.
Also tryBook logic: isAvailable returns Bool. If true -> Some(true), else None.
In Stitch:

ext tryBook(existingBookings: List<Booking>, roomId: Int, time: Time) -> Maybe<Bool> = {
    match isAvailable(existingBookings, time) {
        true => Some(true)
        false => None
    }
}
Wait, Some and None are keywords or constructors?
Reference says sum Maybe<T> = Some(T) | None.
So Some(true) and None are valid.

Let's check fold usage.
fold(xs, init, f).
In isAvailable: bookings |> fold(true, (avail, b) -> avail and not(overlap(b.time, time))).
This is valid.

One more thing: The prompt says "Use these constructs: prod, Maybe, |>".
I should ensure I use them explicitly.
prod used in Time and Booking.
Maybe used in tryBook.
|> used in isAvailable.

Wait, I need to make sure I don't use if.
The reference says "There is no if/else. Conditionals are cond => a | b, or a match block."
So I must replace if with match.

Revised tryBook:

ext tryBook(existingBookings: List<Booking>, roomId: Int, time: Time) -> Maybe<Bool> = {
    let result = isAvailable(existingBookings, time)
    match result {
        true => Some(true)
        false => None
    }
}
Wait, isAvailable returns Bool.
I need to check if I can use match on a Bool. Yes, match x { true => ... | false => ... }.
Or cond => ....
Reference says cond => a | b.
I'll use match for clarity with patterns.

Wait, prod types:
ext prod Time(ext start: Int, ext end: Int)
The fields are exported.
The type itself is exported.

Let's check the test requirements.
"Include test "…" { expect … } items covering the core logic."
I should include tests for overlap, isAvailable, and tryBook.

Final check on constraints:

No loop keywords (used fold which is a combinator, not a keyword).
No if/else (used match).
Booleans are words and, or, not.
No return.
ext for exported items.
Comments explain why.
One fenced block.
One detail: prod vs sum. Time and Booking are records, so prod.
Maybe is a built-in type alias sum Maybe<T> = Some(T) | None.

Wait, I need to make sure fold works with Bool correctly.
avail and not(...).
and takes two Booleans. not returns Bool.
So (avail, b) -> avail and not(...) returns Bool.
fold returns the accumulated value.
Init is true.
If list is empty, returns true. Correct.

One small detail: use List.
The reference says use List imports a module.
I will include use List.

Wait, I need to make sure I don't exceed the size limit (1-4 types, 2-6 functions).
Types: Time, Booking (2 types).
Functions: overlap, isAvailable, tryBook, createBooking (if I add it).
I'll add createBooking to make tryBook more complete.
Wait, tryBook doesn't create the booking, it checks availability.
Maybe I should have a function that does the booking if available?
The prompt says "rooms are booked for exclusive use... core is detecting when two bookings overlap".
So checking overlap is the main task.
I'll stick to 3 functions: overlap, isAvailable, tryBook.
Types: 2.
This fits.

Wait, I should check if Maybe needs to be imported or just used as a type name.
Reference says sum Maybe<T> = Some(T) | None is built-in.
So I don't need use for it.

One more thing: ext prod syntax.
Reference: ext prod Summary(ext count: Int, ext total: Int).
So ext prod Time(ext start: Int, ext end: Int) is correct.

Let's assemble the code.

[assembles the module — see 007.st]

Wait, Maybe<Bool> syntax.
Reference says sum Maybe<T> = Some(T) | None.
So Maybe<Bool> is valid.
Some(true) and None are constructors.
I should ensure I don't use if.
I used match.
I used prod, Maybe, |>.

One detail: fold usage in isAvailable.
bookings |> fold(true, (avail, b) -> avail and not(overlap(b.time, time))).
This is valid Stitch syntax.

Wait, I should check if List needs to be imported. Yes use List.

One detail: ext prod Time(ext start: Int, ext end: Int)
The reference example ext prod Summary(ext count: Int, ext total: Int).
So my syntax is correct.

I will remove comments that are too verbose (max 1-2 lines).
The prompt says "Comments explain why, never what. Keep them to one or two lines."
My comments are a bit long. I'll shorten them.

[reassembles with shorter comments — final output]

Wait, Maybe<Bool> syntax.
Reference says sum Maybe<T> = Some(T) | None.
So Maybe<Bool> is valid.
Some is a constructor.
The reference example test "double doubles" { expect double(2) == 4 }.
It uses expect.

One last check: prod fields are immutable unless marked mut. I didn't use mut.
I used ext on fields for visibility.
I think this is good.

Wait, I need to make sure I don't use if in tryBook. I used match.
Wait, I need to make sure I don't use return.
The block's value is its last expression.
In tryBook, the last expression is the result of match.
So no return needed.

One small thing: ext prod Time(ext start: Int, ext end: Int).
Is ext on fields allowed? Yes, reference example Summary(ext count: Int, ext total: Int).
I'll keep it.

Wait, I should check if I need to import Maybe.
Reference says sum Maybe<T> = Some(T) | None is built-in.
So no import needed.

One detail: prod vs sum. Time and Booking are records, so prod.
Maybe is sum.

I'm ready to write the block.
```

### The load-bearing moment

The `timesOverlap(t1: Time, t2: Time)` helper appears in the first draft and is
dropped during consolidation — but `not(overlap(b.time, time))`, written against
*that* signature, survives into the final program. Neither the later "final check
on constraints" passes nor the comment-shortening pass revisits it.
